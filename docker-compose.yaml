version: '3'
services:
  oracle-db:
    image: oracleinanutshell/oracle-xe-11g:latest
    ports:
      - "1521:1521"
      - "5500:5500"
    deploy:
      replicas: 4
      placement:
        constraints: [node.role==worker]

